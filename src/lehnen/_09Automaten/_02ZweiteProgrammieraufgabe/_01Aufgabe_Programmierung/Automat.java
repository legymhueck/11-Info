/**
 * In dieser Programmieraufgabe ist der Zustand nicht mehr ein Integer-Wert, sondern ein Objekt.
 * - deklariere zunächst ein finales Attribut namens z0 vom Typ Zustand
 * - Schreibe einen Konstruktor für die Klasse
 * - Ein neues Objekt vom Typ Zustand wird erzeugt und in der Variablen z0 gespeichert.
 * - Mache dies analog für die Zustände z1 bis z3, die aber lokable Variablen sind.
 * - setze die Folgezustände für z0 bis z3 mittels der Methode setzeFolgeZustaende,
 * die zwei Parameter vom Typ Zustand entgegennimmt.
 * <p>
 * - entwickele die Methode pruefeEingabe, die einen String als Parameter entgegennimmt und
 * einen Boolean zurückgibt
 * - über eine for-Schleife und die Methode charAt soll jedes Zeichen der Eingabe überprüft werden.
 *
 * - erzeuge in der mail-Methode ein Scanner-Objekt, das Systemeingaben entgegennimmt.
 * - gibt die Methode pruefeEingabe true zurück, soll "Die Eingabe wurde akzeptiert" ausgegeben werden
 * - ansonsten "Die Eingabe wurde NICHT akzeptiert"
 * <p>
 * -
 */
package lehnen._09Automaten._02ZweiteProgrammieraufgabe._01Aufgabe_Programmierung;

public class Automat {
}
